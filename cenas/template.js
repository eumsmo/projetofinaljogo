export default class CenaTemplate extends Phaser.Scene {
    constructor() {
        super({
            key: "CenaTemplate"
        });
    }

    // É chamado antes que a cena apareça
    // Pré carregar coisas especificas da cena atual
    preload() {
    
    }

    // Criar as coisas que vão aparecer dentro dessa cena
    // As imagens já foram pre carregadas no preload agora você pode usa-las aqui
    // Ex: colocar o fundo, onde os objetos vão estar na cena, etc
    create() {

    }

    // Chamado a cada frame (30 vezes, 60 vezes por segundo, quanto você definir)
    // Ex: movimentar um personagem ao longo do tempo, verificar tecla pressionada
    update() {

    }
}